// Header is a self-contained container; no public props for now.
// This file is intentionally a placeholder for the convention enforced by the codebase
// (every shared component folder exposes types/<Component>Props.ts).
export type HeaderProps = Record<string, never>
